let initialState = {
  properties: []
};

const crud = (state = initialState, action) => {
  switch (action.type) {
    
    case 'ADD_PROPERTY':
      
      return {
        
      }



    default:
      return state
  }
}

export default crud
